/**
 * @typedef ModuleFederationComponentType
 * @type {object}
 * @property {string} dir - название директории,  в которой находится компонент.
 * @property {RegExp} [excludePattern] - регулярное выражение для исключения подходящих вариантов.
 * @property {boolean} [recursive] - флаг для рекурсивного обхода директории вглубь и регистрации всех компонентов, которые имеют package.json.
 * @property {boolean} [includeCurrentDir] - флаг для рекурсивного обхода директории вглубь и регистрации всех компонентов, которые имеют package.json.
 */

module.exports = {};
