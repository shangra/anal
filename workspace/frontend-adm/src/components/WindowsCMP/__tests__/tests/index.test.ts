/*
Тесты для интерфейсов и типов не имеют смысла писать напрямую через библиотеку Jest, поскольку они являются статическими описаниями структур данных и не содержат исполняемого кода. Однако можно протестировать использование этих интерфейсов и типов в реальных компонентах или функциях, которые зависят от них.

Тем не менее, если нужно проверить сами типы и интерфейсы, то можно использовать TypeScript компилятор (`tsc`) совместно с Jest для проверки типизации во время выполнения тестов. Но такие тесты будут скорее проверкой того, что ваш проект успешно компилируется без ошибок типизации при использовании указанных интерфейсов и типов.

Если всё-таки необходимо создать какие-то формальные тесты, вот пример тестирования использования интерфейса `IWindow`, который демонстрирует его применение:


**Что здесь происходит?**

1. **Первый тест**: Проверяет создание валидного экземпляра типа `IWindow`. Мы создаем экземпляр, соответствующий всем полям интерфейса, и затем проверяем, что он содержит ожидаемые значения.
   
2. **Второй тест**: Создает намеренно некорректный экземпляр, где некоторые обязательные поля отсутствуют или неверны. В реальном проекте такой подход приведет к ошибкам компиляции TypeScript, но мы имитируем ситуацию, когда кто-то пытается передать недопустимые данные.

Эти тесты помогут убедиться, что интерфейс используется корректно и соответствует ожиданиям разработчиков.

Однако важно помнить, что тестирование интерфейсов и типов — это больше проверка правильности использования API и документации, чем полноценное функциональное тестирование.
*/

import { IWindow } from "components/WindowsCMP/__tests__/src/interfaces"; // Путь до вашего index.ts

describe('Testing interfaces and types', () => {
  test('should create a valid IWindow instance', () => {
    const windowInstance: IWindow = {
      key: 'unique-key',
      title: 'Test Window Title',
      content: <div>Content</div>,
      style: { width: '300px', height: '200px' },
      zIndex: 5,
      close: false,
      callback: () => console.log('Callback function'),
      position: 'right',
      isMinimized: false,
      type: 'setting'
    };

    expect(windowInstance).toMatchObject({
      key: 'unique-key',
      title: 'Test Window Title',
      content: <div>Content</div>
    });
  });

  test('should throw an error when creating invalid IWindow instance', () => {
    let errorThrown = false;
    try {
      const invalidWindowInstance: IWindow = {
        key: '',
        title: undefined,
        content: null
      };
    } catch (error) {
      errorThrown = true;
    }

    expect(errorThrown).toBe(true); // Ожидается ошибка из-за неправильных значений полей
  });
});