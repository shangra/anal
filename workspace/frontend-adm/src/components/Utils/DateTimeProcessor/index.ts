import { dateConvertToNeedFormat, getTimezone, formatDate, formatDateTime } from "components/Utils/DateTimeProcessor/utils";

export const DateTimeProcessor = {
    getTimezone,
    dateConvertToNeedFormat,
    formatDate,
    formatDateTime
};

